# Copy this file to `.env` and fill in your values, then `source .env` or rely
# on python-dotenv (loaded automatically). NEVER commit the real .env.

# Required — get a free key at https://app.labelbox.com → Account → API keys
LABELBOX_API_KEY=

# Optional — override the default resource names.
LB_PROJECT_NAME=robot-grasp-annotation
LB_DATASET_NAME=grasp-frames
LB_ONTOLOGY_NAME=grasp-ontology-v1
LB_BATCH_NAME=grasp-batch-001
